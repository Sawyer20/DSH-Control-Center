# 第三方内容与许可说明

本文件说明本仓库里"哪些是自己的、哪些不是"，以及使用它们的前提。

## 1. 代码 → MIT

`*.cs`、`*.cmd`、`*.ps1`、`app.manifest`、`package.json`、`pnpm-workspace.yaml`、`.npmrc`、
`README.md`、本文件：由本仓库作者编写，按 [MIT](LICENSE) 授权。

## 2. 美术素材 → 作者本人所有（AI 生成）

| 内容 | 位置 | 说明 |
|---|---|---|
| 鲸鱼形象 | `whale_base.png`（Header 圆标） | 作者**用 AI 绘图工具（ChatGPT）自行生成** |
| 鲸鱼喷水动画 | 由作者自己生成的 HTML 动画移植而来，成果在 `PetScene.cs`（SVG 几何 1:1 搬进 WPF） | 同上；**不是**任何第三方的素材 |
| 图标 | `app.ico`、`DSH.ico`、`tray_running/starting/error/offline.ico` | 由上面那张形象衍生、同样由作者生成 |
| 界面截图 | `screenshots/*.png` | 本程序自己的界面 |

这些素材随本仓库一并提供，使用条件与代码相同；唯一需要知道的现实差异是：
**不同司法辖区对"纯 AI 生成内容是否受版权保护"的规定不同**（例如美国要求有人类创作成分），
所以作者不对这些图片的可版权性作任何保证——请按你自己的场景评估。

## 3. 名称与品牌

"DSH"、"DeepSeek" 等名称在本仓库里只作**指称性使用**（说明本工具配套哪个后端）。
本仓库是**第三方工具**，与 DeepSeek 官方没有隶属、赞助或背书关系。

## 4. 不随仓库分发的东西

- **DSH 后端本体**（`@deepseek-ai/dsh`）：不在本仓库内。`install-dsh.cmd` 由使用者按 `package.json`
  里的**精确版本号**自行从 registry 安装，其许可与使用条款由提供方决定。
- **`pnpm-lock.yaml`**：**不入库**（也不进发布包）。它会列出后端全部依赖的包名/版本/校验和，
  属于后端自己的依赖图信息；本地保留一份仅供本机复现安装。
- 仓库内没有任何第三方源码副本（历史上的那份后端文件备份已随历史重写移除）。

## 5. 想换掉品牌素材时

- 删掉或替换 `whale_base.png`：Header 圆标会自动退回纯文字的 "DSH" 占位（代码里本来就有这个兜底）；
- 用同尺寸自绘图标替换 `app.ico` / `tray_*.ico`；
- 宠物场景是独立实现（`PetScene.cs` + `PetWindow.cs`），可以在设置页整体关闭，或替换为你自己的场景。
